using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ColorConsts : MonoBehaviour
{
    [SerializeField] public Color NAO_DESCOBERTO;
    [SerializeField] public Color DESCOBERTO;
    [SerializeField] public Color VISITADO;
    [SerializeField] public Color INIMIGO;
    [SerializeField] public Color PLAYER;
    [SerializeField] public Color OBSTACULO;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
